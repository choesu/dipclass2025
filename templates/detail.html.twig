<!DOCTYPE html>
<html lang="en">
{% include 'partials/head.html.twig' %}

<body>
    {% include 'partials/navbar.html.twig' %}
    <div class="container">
    
        <div class="row" mt-4>
        <div class="col-12">
            <h1 class="fs-3 text-center">{{book.Title}}</h1>
        </div>
            <div class="col-4">
            <img class="img-fluid" src="images/{{book.Cover}}">
    </div>
    <div class="col-8">
</body>

</html>